# 🚀 Быстрый старт HyperSniper

## Команды для Git

### TGTON2 (Telegram Bot) — инициализация нового репо

```bash
cd ~/TGTON2

# Инициализация Git
git init
git add .
git commit -m "Initial commit: HyperSniper Bot with Indexer integration"

# Создайте репозиторий на GitHub: https://github.com/new
# Имя: TGTON2

# Подключение к удалённому репозиторию
git remote add origin https://github.com/srobex/TGTON2.git
git branch -M main
git push -u origin main
```

### TGTON2Index (Indexer) — обновление существующего репо

```bash
cd ~/TGTON2Index

# Добавление изменений
git add .
git commit -m "Update: Integration with HyperSniper Bot"
git push origin main
```

---

## Команды для развёртывания на Debian

### Подготовка системы

```bash
# Обновление
sudo apt update && sudo apt upgrade -y

# Установка зависимостей
sudo apt install -y curl git wget nano htop python3.12 python3.12-venv python3-pip redis-server

# Установка Go
wget https://go.dev/dl/go1.23.4.linux-amd64.tar.gz
sudo rm -rf /usr/local/go
sudo tar -C /usr/local -xzf go1.23.4.linux-amd64.tar.gz
echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc
source ~/.bashrc

# Проверка
go version
python3.12 --version
redis-cli ping
```

### Клонирование и настройка

```bash
cd ~

# Бот
git clone https://github.com/srobex/TGTON2.git
cd TGTON2
python3.12 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt

# Настройка (ОБЯЗАТЕЛЬНО отредактируйте!)
nano config/runtime.env

# Инициализация БД
python -m bot.scripts.init_db

# Индексер
cd ~
git clone https://github.com/srobex/TGTON2Index.git
cd TGTON2Index

# Настройка (ОБЯЗАТЕЛЬНО отредактируйте!)
nano config.yaml
```

### Запуск вручную (для тестирования)

```bash
# Терминал 1: Бот
cd ~/TGTON2
source .venv/bin/activate
python -m bot.main

# Терминал 2: FastAPI
cd ~/TGTON2
source .venv/bin/activate
uvicorn bot.web.app:app --host 0.0.0.0 --port 8000

# Терминал 3: Индексер
cd ~/TGTON2Index
go run ./cmd/indexer --network=mainnet
```

### Запуск через systemd (для продакшена)

```bash
# Создание сервисов
sudo nano /etc/systemd/system/hypersniper-bot.service
sudo nano /etc/systemd/system/hypersniper-api.service
sudo nano /etc/systemd/system/hypersniper-indexer.service

# Содержимое — см. docs/РАЗВЁРТЫВАНИЕ_ПОЛНЫЙ_СТЕК.md

# Запуск
sudo systemctl daemon-reload
sudo systemctl enable hypersniper-bot hypersniper-api hypersniper-indexer
sudo systemctl start hypersniper-bot hypersniper-api
sleep 5
sudo systemctl start hypersniper-indexer

# Проверка
sudo systemctl status hypersniper-bot hypersniper-api hypersniper-indexer
```

---

## Проверка работы

```bash
# 1. Telegram бот
# Напишите /start боту

# 2. FastAPI health
curl http://localhost:8000/api/indexer/health

# 3. Логи
sudo journalctl -u hypersniper-bot -f
sudo journalctl -u hypersniper-api -f  
sudo journalctl -u hypersniper-indexer -f

# 4. Тестовый webhook
curl -X POST http://localhost:8000/api/indexer/event \
  -H "Content-Type: application/json" \
  -d '{"event":"jetton_minter_deployed","minter_address":"EQTest","jetton":{"symbol":"TEST"}}'
```

---

## Важные файлы конфигурации

| Файл | Описание |
|------|----------|
| `TGTON2/config/runtime.env` | Настройки бота (токен, Redis, etc.) |
| `TGTON2Index/config.yaml` | Настройки индексера (webhook, Telegram) |

---

## Полезные команды

```bash
# Перезапуск всего
sudo systemctl restart hypersniper-bot hypersniper-api hypersniper-indexer

# Остановка всего
sudo systemctl stop hypersniper-indexer hypersniper-api hypersniper-bot

# Обновление кода
cd ~/TGTON2 && git pull && source .venv/bin/activate && pip install -r requirements.txt
cd ~/TGTON2Index && git pull
sudo systemctl restart hypersniper-bot hypersniper-api hypersniper-indexer

# Очистка Redis кэша
redis-cli FLUSHDB
```

---

## Документация

- [Полная инструкция по развёртыванию](docs/РАЗВЁРТЫВАНИЕ_ПОЛНЫЙ_СТЕК.md)
- [Интеграция с индексером](docs/ИНТЕГРАЦИЯ_ИНДЕКСЕР.md)
- [Инструкция по боту](ИНСТРУКЦИЯ_ЗАПУСК.md)
- [Roadmap](ROADMAP.md)

