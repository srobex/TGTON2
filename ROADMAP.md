# Roadmap HyperSniper MVP

## Этап 0 — Базовая инфраструктура
- ✅ Развернута модульная структура проекта (bot/, services/, handlers/, plugins/, web/, locales/ и т.д.).
- ✅ Созданы `config/settings.py`, общий `AppSettings` и i18n-движок `bot/utils/i18n.py`.
- ✅ Подготовлены `requirements.txt`, `pyproject.toml`, `.env.example`, README на русском.
- ✅ Добавлены базовые локали `locales/ru.json`, `locales/en.json`.

## Этап 1 — TON Core Services (MVP)
- ✅ `TonDirectClient` (HTTP RPC + WebSocket).
- ✅ Safety Checker (<600 мс, simulate_tx, honeypot, liquidity/volume, smart money).
- ✅ Gem Scanner (поиск JettonMinter, фильтры, теги, подписчики).
- ✅ Swap Service (Quick Buy/Sell, take-profit / анти-раг).
- ✅ Ton Connect backend и реферальный сервис.
- ✅ Loader с инициализацией сервисов.
- ✅ Подключены middlewares (i18n, throttling, db, errors).
- ✅ Настроены кеши (memory/Redis) и автозагрузка плагинов цепей.

## Этап 2 — Telegram UX
- ✅ Core handlers (`/start`, `/menu`, `/hot`) и Gem Hunter UI.
- ✅ TON handlers (`/check`, `/buy`, `/sell`).
- ✅ Добавлены хендлеры Ton Connect (/connect, /wallet), реферальный интерфейс `/referral`, `handlers/ton/positions.py`.
- ✅ Расширены клавиатуры Gem Hunter и уведомления (watchlist, auto-sell push).

## Этап 3 — Данные и хранение
- ✅ Созданы SQLModel-модели (`User`, `Settings`, `Referral`, `Position`, `GemCache`) + Alembic окружение.
- ✅ Настроены `database/` скрипты (`bot/scripts/init_db.py`, migrations) и `db.py` подключает метаданные.
- ✅ Ton Connect, реферальные данные и позиции (auto-sell правила) сохраняются в БД.
- ✅ SafetyChecker/GemScanner переключены на Redis совместимый кеш + GemCache upsert.

## Этап 4 — Mini App + Ton Connect 2.0
- ✅ Backend endpoints (FastAPI `/api/ton-connect/*`, JWT авторизация).
- ✅ Настроен scaffold `web/mini_app` + `web/manifest.json`.
- ✅ Добавлен безопасный JWT payload для Ton Connect Mini App.

## Этап 5 — Расширенный функционал MVP
- ✅ Автопродажа: price feed сервис, команды /autotp и /autooff, хранение правил в БД.
- ✅ «Мои позиции» с P&L (расчёт по цене, вывод в /positions).
- ✅ Реферальный UI: команда /referral с метриками + уведомления о новых рефералах.
- ✅ Улучшения Alpha Scanner: фильтры, сортировка, broadcast топов с кнопками.
- ✅ API/webhook для внешних интеграций (REST `GET /api/gem/top`, `POST /api/webhooks` + push).

## Этап 6 — Плагины и масштабирование
- ✅ Интегрирован `plugins_loader`, подготовлены Solana/Base заготовки.
- ✅ Определены интерфейсы для цепей и план масштабирования (Redis, воркеры).

## Этап 7 — Продакшн
- ✅ Логи/метрики (loguru JSON sink, план Prometheus), тесты/CI (pytest + Actions).
- ✅ Документация (README, `docs/production.md` с гайдлайнами).
- ✅ Scaffold для CI/CD и Docker/Kubernetes (описано в `docs/production.md`).

## Этап 8 — Интеграция с HyperSniper Indexer ✅ NEW
- ✅ Добавлен endpoint `/api/indexer/event` для приёма webhook от индексера.
- ✅ Добавлена настройка `TON__USE_WEBSOCKET` для отключения WebSocket toncenter.
- ✅ Настроен общий Redis кэш между ботом и индексером.
- ✅ Pydantic модели для валидации webhook payload от индексера.
- ✅ Документация по интеграции (`docs/ИНТЕГРАЦИЯ_ИНДЕКСЕР.md`).
- ✅ Инструкция по развёртыванию полного стека (`docs/РАЗВЁРТЫВАНИЕ_ПОЛНЫЙ_СТЕК.md`).

---

## Следующие шаги (Post-MVP)

### Этап 9 — Оптимизация и мониторинг
- 🔲 Prometheus метрики для бота и индексера
- 🔲 Grafana дашборды
- 🔲 AlertManager для уведомлений о падении
- 🔲 Логирование latency интеграции бот↔индексер

### Этап 10 — Автоматизация торговли
- 🔲 Auto-buy при обнаружении токена индексером
- 🔲 Настраиваемые условия автопокупки (min_liquidity, max_latency, etc.)
- 🔲 Очередь ордеров через Redis

### Этап 11 — Масштабирование
- 🔲 Kubernetes deployment
- 🔲 Горизонтальное масштабирование бота
- 🔲 Свой liteserver для минимальной задержки
- 🔲 Geo-distributed индексеры

---

*Обновлено: декабрь 2024*
