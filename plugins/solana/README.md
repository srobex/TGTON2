# Solana плагин (заготовка)

1. Реализуйте сервисы в `plugins/solana/services/`.
2. Зарегистрируйте роутеры/хендлеры в `init_plugin(context)`.
3. Используйте `context["services"]`/`context["settings"]`, чтобы переиспользовать core-слои.




