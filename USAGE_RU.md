## HyperSniper: как использовать бота

HyperSniper — это Telegram-бот для быстрого и безопасного трейдинга в экосистеме TON. Ниже — практическое руководство: что бот умеет, какие задачи решает и как извлечь максимум пользы.

### 1. Старт и базовые действия
- **Подключение**: напишите `/start`, чтобы получить приветствие и (при необходимости) активировать реферальную ссылку `ref_<id>`.
- **Главное меню**: команда `/menu` показывает основные разделы и кнопку переключения языка (RU/EN).
- **Языки**: бот автоматически определяет язык, но позволяет вручную выбрать RU или EN.

### 2. Поиск перспективных токенов
- **Gem Hunter / Alpha Scanner** (`/hot`, `/gem`): бот отслеживает JettonMinter в реальном времени, показывает топ-10 горячих токенов и теги `Smart money inside`, `LP burned`, `New`.
- **Подписка на токен**: кнопка «👀 Подписаться» в карточке токена включает push-уведомления, когда актив возвращается в топ.
- **Гибкие фильтры**: команда `/gemfilters score=70 lp=1 smart=2 sort=volume` позволяет управлять скорингом, ликвидностью и сортировкой.
- **Broadcast топов**: `/gemfeed_on` и `/gemfeed_off` включают автопубликацию рейтинга в личку.

### 3. Проверка безопасности
- **Safety Checker** (`/check <jetton>`): за <600 мс показывает симуляцию транзакции, проверяет honeypot, ликвидность, объёмы и активность «умных денег».
- **Автоуведомления**: если токен отмечен как опасный, бот выдаёт подсказки по снижению риска (например, рекомендует минимизировать объём сделки).

### 4. Сделки и управление позициями
- **Quick Buy / Sell** (`/buy`, `/sell`): non-custodial инструкции, которые запускают сделку напрямую в вашем кошельке.
- **Take-profit и анти-раг**: можно задать правила, при которых бот автоматически пересылает транзакцию на продажу при достижении цены.
- **Auto TP**: `/autotp <адрес> <tp_usd> [stop_usd]` активирует правило; `/autooff <ID>` выключает.
- **Мониторинг позиций** (`/positions`): показывает все активные правила auto-sell и текущий P&L.
- **Уведомления**: каждое срабатывание правила сопровождается личным сообщением с деталями сделки.

### 5. Ton Connect и Mini App
- **Подключение кошелька** (`/connect`, `/wallet`): выдаёт ссылку Ton Connect 2.0, показывает статус кошелька и кнопки «Обновить»/«Отключить».
- **JWT для Mini App**: после `/connect` бот отправляет токен, который Mini App использует для запросов (например, к `/api/gem/top`).
- **Mini App**: фронтенд (React/Vite) отображает портфель, горячие токены и быстрые кнопки покупок прямо в интерфейсе TON.

### 6. Реферальная программа
- **Команда** `/referral`: статистика приглашений, обороты и личная deep-link ссылка.
- **Omniston payload**: встроенная комиссия 0.8–1% автоматически распределяется между реферерами.
- **Уведомления**: бот сообщает о новых рефералах и начислениях, чтобы вы понимали эффективность промо.

### 7. Работа с несколькими цепями
- **Модульные плагины**: HyperSniper готов к подключению Solana, Base и других сетей — новые плагины подгружаются автоматически при старте.
- **Единый интерфейс**: команды и структуры одинаковы для всех цепей, поэтому не нужно переучиваться при добавлении сети.

### 8. Локальные настройки и кеш
- **Throttling**: бот ограничивает частоту команд (по умолчанию 0.5 с), чтобы защититься от спама.
- **Кеш**: автоматически работает в памяти или в Redis, ускоряя ответы Gem Scanner и Safety Checker.
- **База данных**: все настройки пользователя, рефералы, позиции и кеш токенов надёжно сохраняются.

### 9. Как получить максимум пользы
1. **Следите за топом**: держите включённым `/gemfeed_on`, чтобы не пропускать горячие токены.
2. **Включайте подписки** на интересные активы — бот предупредит о повторном появлении в трендах.
3. **Проверяйте безопасность** каждого jetton через `/check`, особенно перед крупной покупкой.
4. **Автоматизируйте выход** с помощью `/autotp`, чтобы не сидеть у экрана.
5. **Используйте Mini App** для визуального контроля портфеля и быстрых операций.
6. **Развивайте сеть** через `/referral`, если планируете монетизировать трафик.

### 10. Когда стоит подключить доп. инфраструктуру
- **Redis**: включите, когда возрос поток запросов к Gem Scanner, чтобы держать время ответа <600 мс.
- **Backend Mini App**: запустите `uvicorn bot.web.app:app --reload --port 8000`, чтобы пользователи Ton Connect видели обновлённые данные.
- **Мониторинг и логи**: используйте `bot/logging_config.py` и будущий Prometheus exporter (`/metrics`), если выводите бота в прод.

---

HyperSniper решает две ключевые боли: скорость реакции на новые токены и прозрачное управление риском. Подключите кошелёк, настройте фильтры, включите авто-продажи — и бот станет вашим «радаром» по мемкоинам TON без необходимости вручную мониторить десятки каналов.

